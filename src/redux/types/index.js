import { MODAL } from './modalTypes';
import { AUTH } from './authTypes';
import { CHARACTERS } from './charactersTypes';
import { USERS } from './userTypes';
import { PHOTOS } from './photosTypes';

export const types = {
	MODAL,
	AUTH,
	CHARACTERS,
	USERS,
	PHOTOS,
};
